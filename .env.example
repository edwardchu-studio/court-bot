# 复制为 .env 并填真实值 (chmod 600 .env)
# 从 ~/.openclaw/.env 可以借用同一个 bot

TG_BOT_TOKEN=
TG_CHAT_ID=6031815881

# 可选: civicpermits 登录凭据 (用于 login_persist.py 自动登录)
# 若网站有 captcha 留空 → login_persist.py 改为 headed 手动登录模式
CIVIC_USERNAME=
CIVIC_PASSWORD=
